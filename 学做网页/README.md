# 学做网页

HTML/CSS 基础练习与小案例。

## 预览
- 直接双击 `*.html` 用浏览器打开即可。
- VS Code 用户可安装 Live Server 插件，右键 HTML 选择“Open with Live Server”。

## 结构
- 页面文件位于本目录及其子目录；
- 样式文件通常位于 `css/` 子目录（按示例课程组织）。

## 备注
- 静态页面无需构建工具；若引用了外部资源，请保持相对路径结构不变。